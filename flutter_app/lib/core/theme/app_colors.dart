import 'package:flutter/material.dart';

/// Paleta SpiderZone (dark-first, terrarium vibe).
abstract final class AppColors {
  // Tło
  static const background = Color(0xFF0E1411);
  static const surface = Color(0xFF17211B);
  static const surfaceElevated = Color(0xFF203027);

  // Akcenty
  static const primary = Color(0xFF6FBF73);
  static const primaryHover = Color(0xFFA5D6A7);
  static const accentOrange = Color(0xFFD98E32);
  static const accentOrangeHover = Color(0xFFE1A24F);

  // Tekst
  static const textPrimary = Color(0xFFE7EEE9);
  static const textSecondary = Color(0xFF9DAEA4);

  // Stany
  static const danger = Color(0xFFD65A4A);
  static const dangerHover = Color(0xFFE17263);
}
