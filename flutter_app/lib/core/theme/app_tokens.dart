/// Design tokens: spacing, radius, animacje.
abstract final class AppTokens {
  static const radiusCard = 14.0;
  static const radiusButton = 12.0;
  static const radiusChip = 10.0;

  static const paddingScreen = 16.0;
  static const paddingCard = 14.0;

  static const durationFast = Duration(milliseconds: 200);
  static const durationNormal = Duration(milliseconds: 280);

  static const scaleCardHover = 1.03;
}
